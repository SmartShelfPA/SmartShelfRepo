appId: ca.com.smartshelf.desktop
productName: SmartShelf
copyright: Copyright © SmartShelf
# Desktop package.json has no Expo deps — only electron-updater + shell files.
electronVersion: 37.10.3
npmRebuild: false
nodeGypRebuild: false
directories:
  # Windows builds override this to %LOCALAPPDATA%\SmartShelf\desktop-release
  # so OneDrive/Cursor cannot lock win-unpacked.tmp (EBUSY).
  output: ../desktop-release
  buildResources: build
files:
  - main.js
  - preload.js
  - downloadsStore.js
  - autoUpdater.js
  - package.json
extraMetadata:
  main: main.js
publish:
  provider: github
  owner: SmartShelfPA
  repo: SmartShelfRepo
  releaseType: release
extraResources:
  - from: ../dist
    to: web-dist
  - from: build
    to: build
win:
  icon: build/icon.png
  target:
    - target: nsis
      arch:
        - x64
  artifactName: SmartShelf-Setup.${ext}
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
mac:
  icon: build/icon.png
  target:
    - dmg
  category: public.app-category.education
  artifactName: SmartShelf.${ext}
dmg:
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications
